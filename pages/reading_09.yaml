title:      "Reading 09: Multilayer Perceptrons, Gradient Descent, and Backpropagation"
icon:       fa-book
navigation:
  - name: "Home"
    link: "index.html"
    icon: "fa-gavel"
internal:
external:
    resources:  'csv:static/csv/resources.csv'
body:       |

    ## Multi-Layer Perceptrons (FFNs)

    <table>
        <tr>
            <td colspan="3">
                {% set resources =  page.external.get('resources', {}).get(lecture_id_for('Multi-Layer Perceptrons (FFNs)'), [])%}
                {% include "resource_sections.tmpl" %}
            </td>
        </tr>
    </table>

    ## Gradient Descent and Backpropagation

    <table>
        <tr>
            <td colspan="3">
                {% set resources =  page.external.get('resources', {}).get(lecture_id_for('Gradient Descent and Backpropagation'), [])%}
                {% include "resource_sections.tmpl" %}
            </td>
        </tr>
    </table>

    ## Quiz